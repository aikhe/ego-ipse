---
trigger: always_on
---

# Rules
- Use the best practices for writing maintainable and clean code
- Avoid making the code base bloated
- Remove any unecessary code if you encounter any
- Don't add any edge cases and error handling if its not needed
- Avoid repeating code
- This is a svelelte v5.51.0 projects so make sure youre using the updated syntax etc.
- This is a svelte project so make sure youre using the best practices of svelelte
- Always follow the DRY and KISS method
- Use the BEM naming convention in writing css
- Make sure the lsp doesnt throw any warnigs, errors, etc. like:

Diagnostics:
1. Each block should have a key [svelte/require-each-key]

Diagnostics:
1. Each block should have a key [svelte/require-each-key]
2. Unexpected href link without resolve(). [svelte/no-navigation-without-resolve]

Diagnostics:
1. Unexpected any. Specify a different type. [@typescript-eslint/no-explicit-any]

Diagnostics:
1. Unexpected href link without resolve(). [svelte/no-navigation-without-resolve]

and many more.