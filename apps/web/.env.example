# GitHub personal access token (classic or fine-grained with public repo read access)
# Used by /api/github-stats to fetch profile stats with higher rate limits.
# Without this, unauthenticated requests are limited to 60/hr.
GITHUB_TOKEN=
